package sml;

/**
 * Interface to name registers in the Simple Machine Language (SML).
 * It should be implemented using Enum for each register name.
 */
public interface RegisterName {
    String name();
}
